# --- secrets -------------------------------------------------------------
.env

# --- python --------------------------------------------------------------
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# --- tooling caches ------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# --- local services ------------------------------------------------------
qdrant_storage/

# --- deferred: pending updates, to be pushed in a later commit -----------
CLAUDE.md
RESULTS.md
docx/
data/documents/
data/fixtures/
# Ignore docs/ contents rather than the directory itself: git will not
# descend into an excluded directory, so a negation inside one never fires.
docs/*
!docs/images/
!docs/design/
!docs/development.md
# Pre-fix capture, kept locally as evidence but not shipped.
docs/images/shot_04_hybrid.png
eval/datasets/meridian_qa.json
eval/results/
